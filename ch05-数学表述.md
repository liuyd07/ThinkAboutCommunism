# 第5章 生产循环的数学表述

> **本章目标**：为前几章的概念建立形式化数学框架，便于精确推理和后续计量验证。

## 5.1 生产过程的向量化

### 5.1.1 生产向量

定义物资空间为 N 维向量空间，其中每个维度对应一种物资类型。

- 输入向量：$x \in \mathbb{R}^N_{\ge 0}$
- 产出向量：$y \in \mathbb{R}^N_{\ge 0}$
- 耗散向量：$d = x - y$（注意：这里 x 和 y 的同一维度含义不同，需区分"消耗"和"产出"）

### 5.1.2 生产过程函数

单个过程：$y = f(x, \tau | M)$

其中：
- $x$：输入物资
- $\tau$：时延
- $M$：生产资料约束（工具、设备、人力）

### 5.1.3 效率指标

- 耗散率：$\eta = |d|/|x|$（特定维度上的净损耗比例）
- 周转时间：$\tau$（从投入到产出的时间）
- 单位时间产出率：$|y|/\tau$

## 5.2 生产链的图表示

### 5.2.1 邻接矩阵

生产链 G = (V, E) 可用邻接矩阵 A 表示：

$A_{ij} = 1$ 如果节点 i 的产出是节点 j 的输入

### 5.2.2 可达性与关键路径

[ToDo：定义链长、关键路径的计算方法]

## 5.3 生产循环的数学定义

### 5.3.1 有向环检测

生产循环等价于有向图中的一个环（强连通分量）。

### 5.3.2 正循环条件

对于环 C = (v₁ → v₂ → ... → v_k → v₁)，定义映射 T 为环一周的净物资变化：

$T(C) = \sum_{i=1}^{k} (y_i - x_i)$

正循环条件：$T(C) > 0$（在某一关键物资维度上）

### 5.3.3 实际案例计算

[ToDo：用简化的煤-钢-机械环进行数值演示]

## 5.4 增长方程

### 5.4.1 指数增长

无约束时，正循环中的关键物资 P 满足：

$\frac{dP}{dt} = \alpha P$

解：$P(t) = P_0 e^{\alpha t}$

### 5.4.2 Logistic 增长

有约束时（资源、市场、技术极限）：

$\frac{dP}{dt} = \alpha P \left(1 - \frac{P}{K}\right)$

其中 K 为承载能力（carrying capacity）。

### 5.4.3 能量约束修正

[ToDo：引入 EROI（能源回报比）对增长方程的修正]

## 5.5 货币化衡量的问题

货币是 N 维生产空间的 **1 维线性嵌入**：

$price: \mathbb{R}^N \to \mathbb{R}_{\ge 0}$

- 优点：大幅降低交易的信息维度，提高协调效率
- 缺点：丢失了物资种类、品质、时变、空间分布的信息
- 这导致货币化衡量可能掩盖生产循环的真实健康状况

## 5.6 与投入产出分析的关系

[ToDo：讨论 Leontief 投入产出模型与本书生产循环图的对应与差异]

---

*本章空缺标记 [ToDo]*
